---
// src/components/Hero.astro
---

<div id="calculadora" class="text-center mb-12 pt-8">
	<div class="relative">
		<!-- Floating elements for visual interest -->
		<div class="absolute -top-4 -left-4 w-20 h-20 bg-gradient-to-br from-emerald-400/20 to-green-500/20 rounded-full blur-xl animate-pulse"></div>
		<div class="absolute -top-2 -right-8 w-16 h-16 bg-gradient-to-br from-blue-400/20 to-cyan-500/20 rounded-full blur-lg animate-pulse delay-700"></div>
		
		<div class="relative z-10">
			<h1 class="text-4xl md:text-6xl font-bold mb-4">
				<span class="bg-gradient-to-r from-emerald-600 via-green-600 to-teal-600 bg-clip-text text-transparent animate-gradient">
					Calculadora Ganadera
				</span>
			</h1>
			<p class="text-xl md:text-2xl text-gray-600 mb-6 max-w-3xl mx-auto leading-relaxed">
				Evalúa el valor comercial de tu ganado en canal con 
				<span class="text-emerald-600 font-semibold">precisión profesional</span>
			</p>
			
			<!-- Stats row -->
			<div class="flex justify-center items-center space-x-8 text-sm text-gray-500 mb-8">
				<div class="flex items-center space-x-2">
					<div class="w-2 h-2 bg-emerald-500 rounded-full animate-pulse"></div>
					<span>Precio actualizado</span>
				</div>
				<div class="hidden md:flex items-center space-x-2">
					<div class="w-2 h-2 bg-blue-500 rounded-full animate-pulse delay-300"></div>
					<span>Datos en tiempo real</span>
				</div>
				<div class="flex items-center space-x-2">
					<div class="w-2 h-2 bg-purple-500 rounded-full animate-pulse delay-500"></div>
					<span>Análisis profesional</span>
				</div>
			</div>
		</div>
	</div>
</div>

<style>
	@keyframes gradient {
		0%, 100% { background-position: 0% 50%; }
		50% { background-position: 100% 50%; }
	}
	
	.animate-gradient {
		background-size: 200% 200%;
		animation: gradient 3s ease infinite;
	}
</style>